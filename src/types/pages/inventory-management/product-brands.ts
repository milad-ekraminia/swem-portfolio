export interface ProductBrand {
  id: string;
  brandName: string;
  concurrencyStamp: string;
}
export interface ProductBrandFormData {
  id: string;
  brandName: string;
  concurrencyStamp: string;
}
